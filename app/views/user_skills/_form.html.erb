
<%= simple_form_for @user_skill do |f| %>
  <%= f.input :skill_id, as: :hidden, value: skill.id %>
  <%= f.input :mode, as: :hidden, value: 'learn' %>
<% end %>

<!--

class UserSkill < ActiveRecord::Base
  belongs_to :skill_category
  has_and_belongs_to_many :skills
end

class SkillCategory < ActiveRecord::Base
  has_many :skills
end

class Skill < ActiveRecord::Base
  has_and_belongs_to_many :user_skills
end


 -->
<%#=  f.collection_check_boxes (:skill, :skill_category_id, SkillCategory.all, :id, :name) %>
