<%= simple_form_for([ match, review ], remote: true) do |form| %>
  <%= form.input :content, as: :text %>
  <%= form.input :rating, as: :integer %>
  <%= form.button :submit, class: "btn btn-sm btn-green" %>

<% end %>
